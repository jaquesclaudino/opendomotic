package com.opendomotic.device.test;

import junit.framework.TestCase;

/**
 *
 * @author Jaques Claudino
 */
public class TestSocketDevice extends TestCase {
    
    public TestSocketDevice(String testName) {
        super(testName);
    }
    
    @Override
    protected void setUp() throws Exception {
        super.setUp();
    }
    
    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
    }

    public void testSocket() throws Exception {
        /*SwitchSocketDevice device = new SwitchSocketDevice();
        device.setHost("localhost");
        device.setPort(10000);
        device.setReadCommand("2");
        device.setWriteCommandOn("1");
        device.setWriteCommandOff("0");
        
        System.out.println(device.getValue());
        
        device.setValue(0);
        device.setValue(1);*/
        
        assert true;
    }
}
